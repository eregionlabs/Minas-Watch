{
  "name": "minas-watch",
  "version": "0.1.0",
  "private": true,
  "description": "Minas Watch MVP bootstrap: schema-first intelligence pipeline scaffolding",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node src/server.mjs",
    "validate:schemas": "node scripts/validate-schemas.mjs",
    "validate:fixtures": "node scripts/validate-fixtures.mjs",
    "test": "npm run validate:schemas && npm run validate:fixtures"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "express": "^4.21.2"
  }
}
